const domainData = {
    data: {
        currentUser: {

        }
    }
}

function domainReducer(state = domainData, action) {
    switch (action.type) {

        default: return state;
    }
}

export default domainReducer;